import * as React from 'react';
import { RouteComponentProps } from 'react-router-dom';

export interface ExploreProps {

}

const Explore = (props:ExploreProps & RouteComponentProps) => {

    return (
        <div>Explore</div>
    );
}

export default Explore;